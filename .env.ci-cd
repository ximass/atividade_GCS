# CI/CD Environment Variables Configuration
# This file documents the environment variables used in the CI/CD pipeline

## GitHub Secrets Required
# These should be configured in GitHub repository settings -> Secrets and Variables -> Actions

### Docker Hub (Optional)
DOCKER_USERNAME=your_docker_username
DOCKER_PASSWORD=your_docker_password_or_token

### Database Credentials
DB_PASSWORD=postgres

### Notification Settings (Optional)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your/webhook/url

### Monitoring (Optional)
NEW_RELIC_LICENSE_KEY=your_new_relic_license_key
DATADOG_API_KEY=your_datadog_api_key

## Environment-Specific Variables
# These are used in the backup scripts and can be overridden

### Staging Environment
STAGING_DB_HOST=177.44.248.74
STAGING_DB_PORT=5432
STAGING_DB_NAME=staging
STAGING_DB_USER=postgres

### Production Environment  
PROD_DB_HOST=177.44.248.74
PROD_DB_PORT=5432
PROD_DB_NAME=production
PROD_DB_USER=postgres

## CI/CD Pipeline Configuration

### Test Configuration
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=testing
TEST_DB_USER=postgres
TEST_DB_PASSWORD=postgres

### Performance Thresholds
STAGING_RESPONSE_THRESHOLD=5.0  # seconds
PRODUCTION_RESPONSE_THRESHOLD=3.0  # seconds

### Backup Configuration
BACKUP_RETENTION_DAYS=7
BACKUP_RETENTION_COUNT=5

### Health Check Configuration
HEALTH_CHECK_RETRIES=10
HEALTH_CHECK_TIMEOUT=15
HEALTH_CHECK_INTERVAL=15

## Docker Configuration
DOCKER_REGISTRY=docker.io
DOCKER_REPOSITORY=your_username/laravel-app

## Security Scanning
TRIVY_SEVERITY=HIGH,CRITICAL
SECURITY_SCAN_ENABLED=true

## Deployment Configuration
DEPLOYMENT_TIMEOUT=600  # 10 minutes
ROLLBACK_ENABLED=true
AUTOMATIC_ROLLBACK=true

## Monitoring Configuration  
MONITORING_ENABLED=true
ALERT_ON_FAILURE=true
PERFORMANCE_MONITORING=true

## Notes:
# - All sensitive values should be stored as GitHub Secrets
# - Environment-specific values can be stored as GitHub Variables
# - Boolean values should be set as "true" or "false" strings
# - Timeouts and thresholds should be numeric values
# - URLs should include the full protocol (http/https)
